import React from "react";
import { CartIcon } from "../Icons";

const CartWidget = () => {
  return (
    <div>
      <CartIcon size="2.5rem" color="blue" />
    </div>
  );
};

export default CartWidget;
